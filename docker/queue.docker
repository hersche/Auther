FROM php:7.3.11-fpm

RUN apt-get update && apt-get install -y default-mysql-client zlib1g-dev git nano zip libpng-dev libzip-dev\
    && docker-php-ext-install gd pdo_mysql zip

WORKDIR /var/www
CMD ["/bin/sh", "./docker/queue.sh"]
